# auto_link-beta
aplicacion de terminal, en phyton, para poder comprovar si un html, tiene URS validas, es decir que funcioen y te lleven a una web real. // A terminal application in Python to check whether an HTML file contains valid URLs — that is, links that work and lead to a real website.
